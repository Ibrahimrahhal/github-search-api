{
  "name": "@ibrahimrahhal/github-search-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --config .prettierrc './**/*.ts' --write",
    "build": "rm -rf dist && tsc",
    "prepublish": "npm run build"
  },
  "keywords": [],
  "author": "",
  "files": ["dist"],
  "main": "dist/src/index.js",
  "license": "ISC",
  "devDependencies": {
    "prettier": "^2.8.4",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "@types/react": "^18.0.28",
    "axios": "^1.3.4"
  }
}
